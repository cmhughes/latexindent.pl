% make trailing comments use an array, as in log file
% make trailing comments use an array, as in log file
% make trailing comments use an array, as in log file
here is a nested test
\begin{one}[optional argument] [\[x^2\]another optional argument][third optional argument]
	to be nested to be nested
	to be nested to be nested
	to be nested to be nested
\end{one}
