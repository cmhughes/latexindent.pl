modifyLineBreaks:
   environments:
       BodyStartsOnOwnLine: 1
       EndStartsOnOwnLine: 1

fineTuning:
  environments:
    begin: |-
      (?x)                      # environment 
        \\begin\{               # regex
          (?<ENVNAME>           # NON default
           [a-zA-Z@\*0-9_\\]+   #
          )                     #
        \}\s*\\label\{[^}]+?\}  #
    end: \\end\{\g{ENVNAME}\}   # note the capture group name
