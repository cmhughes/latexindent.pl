fineTuning:
  commands:
    between: |-          
      (?x)               
      (?:                
        \\[a-z]\/\\[a-z]
        |                
        (?:in|at)           
        |
        \([^)]*?\)
      )                  
noAdditionalIndent:
    foreach: 
        body: 1
