indentPreamble: 0

modifyLineBreaks:
    textWrapOptions:
        columns: 80
        blocksFollow:
           other: |-
             (?x)\\begin\{[^}]*?\}
                 |
                 \\item
                 |
                 \\documentclass\{[^}]*?\}
