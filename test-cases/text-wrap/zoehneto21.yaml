indentPreamble: 1

modifyLineBreaks:
    textWrapOptions:
        columns: 80
        blocksFollow:
           other: |-
             (?x)\\begin\{[^}]*?\}
                 |
                 \\item
                 |
                 \\documentclass\{[^}]*?\}
