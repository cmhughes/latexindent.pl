defaultIndent: "    "
modifyLineBreaks:
  textWrapOptions:
    columns: 60
    when: "after"
    blocksFollow:
      other: |
        (?x)
        \\begin\{[a-z]+?\}
        |
        \\end\{[a-z]+?\}
