indentPreamble: 1

modifyLineBreaks:
    textWrapOptions:
        columns: 20
        blocksFollow:
           other: |-
             (?x)\\begin\{[^}]*?\}
                 |
                 \\item
                 |
                 \\documentclass\{[^}]*?\}
