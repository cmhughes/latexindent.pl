specialBeginEnd:
    While:
        begin: \\While(?:\{[^}]+\})?
        end: \\EndWhile
    If:
        begin: \\If(?:\{[^}]+\})?
        middle: \\ElsIf
        end: \\EndIf
        nested: 1
fineTuning:
  commands:
    name: (?!begin|end|If|While|ElsIf)[+a-zA-Z@\*0-9_\:]+
