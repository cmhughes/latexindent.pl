    Indeed, $\tilde{s}$ is a change of parameter since $\tilde{s}(0) = \alpha$ and $\forall t\in[0,L)$,
    if $t$ is a discontinuity,
    then by \cref{lma: psuedo inverse of increasing function} $\varphi$ is constant on $\interior{I}_t$ and
    \begin{equation*}
        \forall \tau, \tau'\in \interior{I}_t,
        \int^{\tau}_{\tau'} \abs{\dif \gamma} = s(\tau)-s(\tau') = 0 \mtext{hence} \gamma(\tau) = \gamma(\tau')
    \end{equation*}  and for any
    $\tau\in (\tilde{s}(L), \beta]$,
