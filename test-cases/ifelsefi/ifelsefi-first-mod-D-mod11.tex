here is some text \ifodd\value{totalchapters}=#1
	\typeout{Total Chapters match auxilary file (#1)}
\else
	\typeout{Warning: total Chapter count updated from \the\value{totalchapters} to #1-- recompile to fix} \fi
after text
after text

\ifnum
	something\else something else
\fi
some more text
